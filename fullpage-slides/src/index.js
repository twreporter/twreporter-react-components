import FullpageSlides from './components/slides-wrapper'
import * as CONTENT_TYPES from './constants/content-types'
import * as BG_TYPES from './constants/background-types'
import * as TEXT_BOX_POSITIONS from './constants/text-box-positions'

const FullpageSlidesConstants = {
  BG_TYPES,
  CONTENT_TYPES,
  TEXT_BOX_POSITIONS,
}

export {
  FullpageSlides,
  FullpageSlidesConstants,
}
